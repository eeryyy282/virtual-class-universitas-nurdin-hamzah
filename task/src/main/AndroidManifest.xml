<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools">

    <dist:module
        dist:instant="false"
        dist:title="@string/title_detailtask">
        <dist:delivery>
            <dist:on-demand />
        </dist:delivery>

        <dist:fusing dist:include="true" />
    </dist:module>

    <application android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".presentation.submitedtask.detailsubmitedtask.DetailSubmittedTaskActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.edittask.EditTaskActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.submitedtask.SubmittedTaskActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.detailtask.DetailTaskActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            tools:ignore="DiscouragedApi,LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />

                <data
                    android:host="detail_task_activity"
                    android:scheme="task" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.submittask.SubmitTaskActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            tools:ignore="DiscouragedApi,LockedOrientationActivity" />
    </application>

</manifest>