<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution">

    <dist:module
        dist:instant="true"
        dist:title="@string/title_authentication">
        <dist:delivery>
            <dist:install-time />
        </dist:delivery>

        <dist:fusing dist:include="false" />
    </dist:module>

    <application android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".register.RegisterActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data
                    android:host="register"
                    android:scheme="authentication" />
            </intent-filter>
        </activity>
        <activity
            android:name=".login.mahasiswa.LoginMahasiswaActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data
                    android:host="login"
                    android:scheme="authentication" />
            </intent-filter>
        </activity>
        <activity
            android:name=".login.dosen.LoginDosenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data
                    android:host="login_as_dosen"
                    android:scheme="authentication" />
            </intent-filter>
        </activity>
    </application>

</manifest>